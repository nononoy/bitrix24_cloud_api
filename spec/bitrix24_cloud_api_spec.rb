require 'spec_helper'

describe Bitrix24CloudApi do
  it 'has a version number' do
    expect(Bitrix24CloudApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
